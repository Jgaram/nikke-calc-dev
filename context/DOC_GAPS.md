# Known Documentation Gaps

각 항목은 사연이 있어 당장 처리하지 않음. 해소 시 이 목록에서 제거.

- **`data/` README 없음** — `base_stat_tables/` 하위 각 JSON 스키마 및 읽는 법 미문서화
- **루트 진입점 안내 없음** — "처음 어디서 시작하나"를 루트 레벨에서 안내하는 문서 없음. 빠른 시작 코드는 `calculator/README.md` 안에만 있음
- **`sim_result.py`가 `calculator/README.md` 파일 구성 표에 누락** — `analyze_damage()`, `print_team_analysis()` 등 분석 함수 위치 불명확
