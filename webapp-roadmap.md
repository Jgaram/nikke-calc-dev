# 웹앱 로드맵

폰에서 솔로레이드 5개 덱을 편성·계산하고 기록하며, 실측 딜량을 기준으로 육성 효율을
비교하는 웹앱을 만든다. 이 문서에는 **아직 구현하지 않은 결정과 작업만** 남긴다.
게임 사실은 `context/`, 구현 상태는 코드가 정본이다.

## 1. 남은 작업 순서

1. **기록 탭** — 시즌·덱·시도를 입력·조회하고 내보낸다.
2. **편성 탭 편의 기능** — 급하지 않은 잔여 항목을 처리한다.
3. **오버로드 옵션 투자 UI** — `overload/`의 계산을 웹앱에 연결한다.

첫 로딩을 완전 오프라인으로 만들려면 Pyodide를 캐시하는 서비스 워커가 필요하다. 선택
항목이며 위 작업보다 우선하지 않는다.

웹앱을 공유할 만한 상태가 되면 배포 레포(`nikke-calc`)로 옮긴다. `publish` 스킬의 3단계
제외 목록에서 `web`·`.github`를 빼고, `.github/workflows/pages.yml`의 `if`를 되돌린 뒤 dev
레포의 Pages를 끈다. 그전까지 배포 레포에는 웹앱을 올리지 않는다.

## 2. 기록 탭

- 시즌 → 덱 → 시도 구조로 실측 딜량을 입력·조회한다.
- 브라우저 저장소의 기록과 개인 데이터를 파일로 내보내고 복원할 수 있어야 한다.
- 계산 결과에 저장된 프로필 수집 시각을 함께 보존해 어느 육성 시점의 예측인지 알 수 있게
  한다.
- 기록 탭이 생기면 투자 효율의 억 단위 기준선을 계산 딜량에서 해당 덱의 실측 딜량으로
  바꾼다.

육성 효율의 환산식은 다음과 같다.

```
예상딜 = 실측딜 × (1 + 계산상 Δ%)
```

스킬 레벨·장비 옵션·큐브처럼 곱연산인 축에는 잘 맞는다. 버스트 쿨감처럼 사이클 수를
바꾸는 축은 실전 손실이 비례하지 않아 과대평가될 수 있으므로 결과에 표시한다.

아직 정할 것:

- 기록 백업 주기와 수단
- 공개 상태에서 기록 탭을 어떻게 다룰지

## 3. 편성 탭 편의 기능

- 덱 이름·메모
- 덱 순서 바꾸기
- 덱 단위 복제
- 니케 풀 정렬 선택
- 계산 실패 메시지에서 원인 캐릭터 지목
- PC 결과(`.report-work/*/result.data.json`)를 빌드 시 초기 캐시로 포함
- 니케 풀 기본 필터 결정: 파싱된 전체 / 보유분 / 육성된 것만

## 4. 오버로드 옵션 투자 UI

오버로드 옵션은 목표치까지 정확히 올리는 축이 아니라 기대값이 높은 중단 전략을 고르는
문제다. 계산의 정본은 `overload/`, 근거와 검증 수치는 `overload/README.md`다.

### UI 계약

- 목표 옵션 조합을 받지 않고 현재 옵션을 입력받아 **그만두기 / 효과변경 / 수치변경**과
  잠금 조합 중 최선을 고른다.
- 유저 입력은 λ가 아니라 **쓸 모듈 개수**다. 효율 곡선에서 기대 모듈 합이 예산과 맞는 λ를
  역산한다.
- 출력은 후보 부위 전체의 우선순위와 부위별 전략이다. 순위와 전략은 같은 λ에서 구한다.
- λ가 맞추는 값은 하드 상한이 아니라 기대 예산임을 표시한다.
- 기대 모듈만으로 긴 꼬리를 숨기지 않도록 `overload.rollout`의 분위수를 함께 보여준다.
- 한계가치 측정 결과를 먼저 보여주고 정책 DP는 뒤에서 계산한다.
- 폰은 거친 레벨 버킷을 사용한다. 정확 계산은 PC에 남긴다.

### 계산·캐시 계약

- 한계가치 캐시는 `덱 + 상대 + 육성`, 정책 DP 캐시는 `캐릭터 + λ` 단위로 둔다.
- λ를 이분법으로 반복 계산하지 않는다. 캐릭터별 λ 격자의 `(λ → 기대 모듈)` 곡선을 미리
  계산하고 보간해 뒤집는다.
- 같은 캐릭터의 부위는 서로 가치를 바꾸므로 결과는 한 시점의 스냅샷이다. 한 부위를 끝내면
  다시 계산한다.

### 구현 전 검증

현재 부위의 가치는 **그 부위를 뺀 나머지 옵션 줄**을 배경으로 계산해야 한다. 이미 붙은
옵션을 두 번 세지 않도록 `worth(전체) - worth(해당 부위를 뺀 상태)` 방식으로 보정하고
검증한 뒤 웹앱에 연결한다.

## 5. 세션 재개 지침

1. `AGENTS.md` 다음 이 문서를 읽는다. 게임 메커니즘이 필요할 때만 관련 `context/`로 간다.
2. 웹앱 빌드·실행 방법은 `web/build.py` 상단 docstring을 따른다.
3. 웹앱 작업 중 계산기 코드·데이터의 이상을 발견하면 별도 버그로 다룬다.
4. 프로필·기록·세션 쿠키는 커밋하지 않는다.
5. 미구현 결정이 구현되면 이 문서에서 제거한다.
