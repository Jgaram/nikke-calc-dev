# 웹앱 로드맵

폰에서 덱을 짜고 계산하고 기록하는 웹앱을 만든다. 여러 세션에 걸친 작업이라
**결정과 진행 상태**를 여기에 남긴다.

이 문서는 결정의 정본이다. 게임 사실은 `context/`가, 구현 상태는 코드가 정본이므로
여기에 다시 적지 않는다. 결정이 뒤집히면 이유와 함께 덮어쓴다.

## 1. 배경 — 솔로레이드 운영

한 달에 한 번, 5일간 열린다. **캐릭터가 겹치지 않는 5개 덱**을 편성하고 각 덱의 최고 딜량
합으로 겨룬다. 기간 중 여러 번 도전하거나 모의전으로 연습하면서 최적 조합을 찾아간다.

유저의 실제 흐름은 넷이다.

1. **레이드 전** — 어떤 덱을 짤지 고민한다. 계산기가 예상 딜량을 준다
2. **레이드 중** — PC방에서 플레이하며 개별 덱 딜량을 엑셀에 기록한다 (이번 작업 범위 밖)
3. **조합 확정 후** — 컨트롤 연습과 함께 누구를 더 육성할지 정한다. 계산기가 재화 효율을 준다
4. **종료 후** — 최종 기록을 남긴다

1번이 가장 원하는 기능이고, 그 UI 안에서 4번까지 이어지는 것이 목표다.

## 2. 목표

- 폰에서 드래그로 덱을 여러 개 짜고, **다 짠 뒤 눌러서** 예상 딜량을 받는다
- 솔로레이드 기록(시즌 → 덱 → 시도)을 같은 화면에서 관리한다
- 기록한 실측 딜량을 기준선 삼아 재화 투자 효율을 계산한다

## 3. 하지 않는 것

| 안 하는 것 | 이유 |
|---|---|
| 엑셀 딜량 관리 연동 | 이번 범위 밖. PC방 입력 창구로 현행 유지 |
| 실전 딜량과 절대값 일치 | 보스 소멸·파츠·아픈 공격 엄폐 등 계산 밖 요소가 덱마다 다르다. **비율만** 쓴다 |
| 서버 운영 | 정적 호스팅만 쓴다 |
| 폰에서 조합 탐색 | N덱 최적화는 조합 폭발이라 PC 전용으로 남긴다 |
| 드래그 즉시 계산 | 덱당 수 초가 든다. 계산은 명시적 트리거로만 |

## 4. 확정된 설계 결정

| 결정 | 근거 |
|---|---|
| 계산은 브라우저 안 Pyodide에서 돌린다 | 계산기가 순수 표준 라이브러리라 재작성 없이 그대로 올라간다. JS 재구현은 8천 줄과 파싱 데이터를 영구 이중 관리하게 된다 |
| 계산기 코드·데이터의 정본은 하나 | 웹앱은 `calculator/`·`context/spec.py`·`data/`를 복사하지 않고 빌드 시 번들한다 |
| 개인 데이터는 저장소에 넣지 않는다 | 배포처가 공개 레포다. 프로필·기록은 브라우저 저장소에만 둔다. 협상 대상이 아니다 |
| 계산 결과는 영구 캐시한다 | 기대값 모드는 결정론적이고 폰·PC 값이 완전히 일치한다(§5). 덱 지문을 키로 쓴다 |
| 조합 탐색은 PC에 남긴다 | 기존 최적화 도구를 그대로 쓴다. 폰은 **내가 지정한 덱**을 재는 도구다 |
| 폰↔PC 실시간 동기화 없음 | 단방향 둘로 충분하다. PC→폰은 배포, 폰→PC는 내보내기 파일 |
| Pages는 GitHub Actions가 빌드해 올린다 | 빌드 산출물을 커밋하지 않기 위해서다. Pages는 레포에 있는 파일만 띄우는데 `web/dist/`는 ignore 대상이라, 누군가는 빌드를 해야 한다. 손으로 커밋하면 소스와 산출물이 어긋나고, 정본이 둘로 늘어난다 |
| 개발 중 폰 확인은 로컬 서버로 한다 | `build.py --serve`로 같은 Wi-Fi에서 접속한다(§5 측정도 이 방식). 한 줄 고칠 때마다 배포하지 않는다. Pages는 실사용 통로다 |
| 육성 프로필은 **파일로 넣는다**. 수집은 계속 PC가 한다 | 브라우저가 직접 받을 길이 없다 — blablalink API는 자기 도메인만 CORS 허용하고(실측 2026-08-20: 다른 오리진은 프리플라이트 405) 로그인 세션 쿠키를 요구한다. 서버 프록시를 두면 유저가 세션 쿠키를 남의 서버에 넘기게 되어 위 원칙과 정면 충돌한다. `scraper/profile_fetch.py`가 만든 `profiles/me.json`을 그대로 고른다 |
| 프로필 스위치는 **앱 전역 하나** | 프로필은 내 계정 상태 하나뿐이라 편성안마다 다를 이유가 없다. 약점·코어와 달리 *편성의 전제*가 아니라 *계산의 기준*이다. 넣으면 켜진 채로 시작하고, 끄면 기본 스펙으로 돌아간다 |
| 웹앱은 레벨 정책을 노출하지 않는다 | 언제나 400이다. 솔로레이드가 그렇게 돌기 때문이며, 고정 스펙과 레벨이 같아야 **육성 차이만 남는 비교**가 된다. 프로필의 `sync` 정책과 `_account.synchro_level`은 웹앱에서 쓰지 않는다 |
| 결과 캐시 지문에 **그 덱 5명의 육성 상태와 콘솔**을 넣는다 | 프로필을 갱신하면 낡은 결과가 남는다. 프로필 전체를 해시로 잡으면 한 명만 키워도 25덱이 통째로 무효가 되어 폰에서 3분을 다시 돌려야 한다. 5명분만 잡으면 그 캐릭터가 든 덱만 무효가 된다. 콘솔은 계정 단위라 전체에 걸리는 게 맞다. 큐브 관찰치는 계산에 안 들어가므로 뺀다 |

## 5. 타당성 측정 (2026-08-19)

덱 1개 = 5인 스쿼드, 180초, 기대값 모드, 랩쳐 풍압. 시뮬 본체만 측정.

| 환경 | 덱당 | 비고 |
|---|---|---|
| 네이티브 CPython 3.13 | 3.24초 | 3덱 평균 |
| 데스크탑 브라우저 | 6.26초 | 네이티브의 1.9배 |
| 갤럭시 / Samsung Browser 30 | 6.5~9.5초 | 데스크탑의 1.2~1.3배 |

부팅 2.2~3.5초(첫 방문만, 이후 브라우저 캐시), 번들 압축 0.41MB, 계산기 import 0.3초.
배포 주소에서 캐시 없이 처음 열면 계산 준비까지 7.7초였다(데스크탑, CDN 내려받기 포함).

측정에서 나온 사실 셋:

- **총딜이 네이티브와 완전히 일치한다.** 이식으로 값이 흔들리지 않으므로 캐시를 영구 보관해도
  되고 PC 결과와 폰 결과를 같은 표에 섞어도 된다
- **앱을 전환하면 계산이 3배 이상 느려진다.** 같은 덱이 8.85초 → 26.75초로 관측됐다.
  계산 큐가 도는 동안 화면을 붙잡고(Wake Lock), 큐 상태를 저장해 이탈 후 이어서 돌려야 한다
- 계산기를 한 줄도 고치지 않고 그대로 import된다

스파이크 코드는 레포에 없다 (스크래치패드에서 1회용으로 돌렸다).

## 6. 단계

| # | 단계 | 상태 | 완료 조건 |
|---|---|---|---|
| 0 | 타당성 스파이크 | **완료** (2026-08-19) | 폰 덱당 계산 시간 확보 |
| 1 | 편성 탭 | **완료** (2026-08-19) | 폰에서 덱을 여러 개 짜고 계산해 딜량을 보고, 결과가 캐시된다 |
| 4 | Pages 배포 | **완료** (2026-08-19) | 로컬 서버 없이 폰에서 열린다 |
| 2 | 기록 탭 | **다음** | 시즌·덱·시도를 입력·조회하고 내보낼 수 있다 |
| 1b | 편성 탭 다듬기 | 대기 | 아래 잔여 목록. 급하지 않다 |
| 3 | 육성 탭 + 비용표 확충 | 대기 | 실측 딜량 × 계산상 Δ%로 재화별 투자 순위가 나온다 |

**2단계를 먼저 한다.** 실사용이 시작됐으므로 브라우저 저장소에 실제 데이터가 쌓이기
시작한다. 지금은 그게 날아가면 복구 수단이 없다 — 내보내기가 편의 기능이 아니라
데이터 보전 문제가 됐다. 1b의 편의 기능보다 이게 앞이다.

배포된 주소는 `https://jgaram.github.io/nikke-calc/`다. 배포처가 public 레포여야 하는
이유는 무료 플랜에서 Pages가 public만 지원해서다. 개발 중 확인은 로컬 서버로 하고
(§4) publish는 덩어리로 모아서 한다.

4단계에서 남은 것 하나:

- **첫 로딩에는 인터넷이 필요하다.** Pyodide를 CDN에서 받는다. 한 번 받으면 브라우저 캐시로
  대체로 빠르지만, 신호가 끊긴 상태에서도 확실히 열리게 하려면 서비스 워커가 필요하다
  (선택 항목)

저장소가 주소별로 분리되는 문제는 "개발 주소는 동작 확인용, 실제 편성은 배포 주소에서"로
닫았다.

### 1단계 범위

- 웹앱은 `web/` 아래에 둔다. 빌드 스크립트가 계산기·데이터를 번들한다
- 니케 풀은 파싱된 캐릭터만 선택 가능. 미파싱은 회색 처리한다 (넣어도 계산이 안 된다)
- 드래그로 5칸 덱 슬롯, 덱 추가·복제·삭제
- 덱마다 계산 버튼 → Web Worker 큐 → 결과 표시 + 지문 캐시
- 랩쳐 속성 코드·전투 시간 설정, 캐릭터 중복 경고, 덱 합계
- 육성은 기본 스펙 고정. 프로필 연동은 2단계 이후
- 기록·육성 탭은 뼈대만 두고 비워둔다

### 1b 잔여 (급하지 않다)

1단계는 폰에서 드래그·계산·캐시가 동작하는 것까지 확인했다. 남은 것은 편의 기능이다.

편의 기능 1차는 2026-08-19에 넣었다 — 5덱 기본 편성, 후보 칸, 덱 슬롯과 풀의 초상화 크기
통일, 계산 특이사항 접기, 풀 필터 5축(접이식), 약점 속성 선택, 코어 설정.
여기서 정한 것:

- **후보 칸은 편성이 아니다.** 검색을 매번 반복하지 않으려고 두는 대기석이라 계산에도
  덱 간 중복 판정에도 들어가지 않는다. 상한은 15명 — 5덱 25칸을 다 채울 만큼 담으면
  후보가 아니라 또 하나의 편성이 된다
- **덱 5개는 첫 실행에 한 번만 채운다.** 이후 지운 덱은 지운 대로 둔다. 매번 5개로
  되돌리면 덱 삭제가 동작하지 않는 것처럼 보인다
- **전투 시간은 180초 고정.** 솔로레이드에서 고를 일이 없다. 그 자리를 코어 유무·크기가
  받았다 — 코어는 덱 순위를 뒤집는 축인데(같은 덱 17.1억 → 23.5억) 지금까지 설정에 없었다
- **화면은 약점 속성으로 말한다.** 고르는 것도 강조하는 것도 약점 쪽이다 — 편성에서 쓰는
  값이 그것이라 랩쳐 원속성은 띄우지 않는다. 저장·계산·캐시 지문은 계속 랩쳐 코드를 쓴다
  (계산기 입력이 그쪽이고, 지문이 바뀌면 쌓인 캐시가 날아간다). 속성 없는 랩쳐는 뺐다 —
  솔로레이드에 나오지 않는다
- **초상화 좌상단 배지는 버스트 단계만.** 속성까지 얹으면 배지가 얼굴을 가린다. 속성은
  약점일 때 테두리 색으로 드러나고, 전체 속성은 필터로 본다
- **분류축·상성표·속성색은 빌드 때 `roster.json`으로 내보낸다.** 웹앱이 같은 표를 다시
  적으면 캐릭터나 축이 늘 때 두 곳을 고쳐야 한다. 정본은 `context/roster.py`와
  `calculator/damage.py`다

편의 기능 2차는 2026-08-20에 넣었다 — **편성안**(5덱 한 벌)을 여러 벌 두고, 복제하고,
합계를 나란히 비교한다. 여기서 정한 것:

- **단위는 덱이 아니라 편성안이다.** 합계도 덱 간 중복 판정도 편성안 안에서만 센다.
  다른 벌이 같은 니케를 써도 중복이 아니다 — 실제로 낼 편성은 그중 한 벌뿐이다
- **복제가 이 화면의 핵심 동작이다.** 결과 캐시가 덱 지문(이름·순서·랩쳐·시간·코어)으로
  잡혀 있어 편성안에 매이지 않는다. 한 벌을 복제하고 두 덱만 바꾸면 나머지 세 덱은
  계산 없이 값이 따라오고, `전부 계산`이 바뀐 두 덱만 큐에 넣는다
- **개수 상한은 두지 않는다.** 20벌에서 저장 15.5KB·렌더 3ms였다. 저장되는 것은 이름
  배열뿐이고 계산량은 편성안 수가 아니라 **서로 다른 덱 수**를 따른다
- **후보 칸은 편성안과 무관하게 하나다.** 편성이 아니라 검색 대기석이라 어느 벌을 짜든
  같은 자리에서 꺼내 쓴다
- **상대(약점 속성·코어)는 편성안에 딸린다.** 누구를 상대하느냐가 편성의 전제다 — 같은
  5덱이라도 상대가 다르면 다른 편성안이다. 지문에서 랩쳐·코어 자리가 이제 편성안에서
  오므로, 복제하고 약점만 바꾸면 그 약점으로 돌려본 적 있는 덱만 값이 남는다.
  전역으로 남는 것은 **다음에 만들 편성안의 기본값**뿐이다
- **편성안 드롭다운이 맨 위에 선다.** 그 아래 설정 바(약점·코어)가 이 편성안에 딸린
  값이라, 고르는 것이 먼저 와야 무엇에 대한 설정인지가 보인다
- **약점은 이름 앞의 속성 아이콘이다.** 상대가 빠지면 조건이 다른 두 벌의 합계를 나란히
  놓고 큰 쪽이 좋다고 착각한다. 다만 글자로 적으면 이름 줄이 길어져 정작 이름이 잘린다.
  아이콘을 쓰려고 드롭다운을 직접 만들었다 — 네이티브 `select`는 옵션에 이미지를 못 받고,
  이모지는 기기마다 다른 그림이 나와 설정 바의 아이콘과 따로 논다
- **이름 줄에는 계산 진행도 코어도 넣지 않는다.** 이름 줄은 그 벌이 *무엇인지*(이름·약점·
  합계)만 말하고, *어디까지 했는지*(n/m·계산 중)와 코어는 비교표가 맡는다
- **고르는 것은 드롭다운이 받는다.** 처음엔 칩 줄이었는데 스무 벌에서 가로 2930px가 되어
  목록이 스크롤 뒤에 숨었다. 드롭다운은 세로로 쌓아 한 번에 보여주고, 각 줄에 합계를
  같이 적어 고르는 자리에서 바로 비교가 된다. 높이는 52vh로 묶는다 — 화면을 다 덮으면
  무엇 위에 떠 있는지 알 수 없다
- **이름은 드롭다운 자리에서 바로 고친다.** 기본값 `편성 N`은 벌을 구분하지 못한다 —
  이름이 곧 그 벌이 무엇인지다. `prompt`는 폰에서 투박하고 브라우저에 따라 막혀서 쓰지
  않는다. 같은 이름을 넣으면 뒤에 번호가 붙는다
- **비교표는 남긴다.** 드롭다운은 열어야 보이고 고르면 닫힌다. 최고(★)·중복·진행을 한눈에
  훑는 자리는 따로 필요하다. 최고 표시는 5덱이 다 계산된 벌끼리만 가린다 — 반쯤 계산된
  벌은 합계가 작을 수밖에 없다

편의 기능 3차는 2026-08-20에 넣었다 — **육성 프로필 연동**. 여기서 정한 것:

- **프로필에 없는 캐릭터도 그냥 계산된다.** 미육성(돌파 0 · 스킬 1/1/1 · 장비 미장착)으로
  떨어지므로 "지금 뽑으면 얼마나 나오나"가 그대로 답이 된다. 그래서 풀에서 미보유를 회색
  처리하지 않는다 — 정본은 `context/spec.py`의 `UNGROWN`이고 PC 러너도 같게 돈다
- **낡은 결과는 보여주지 않고 지문으로 가른다.** 결과에 그때의 육성 상태를 함께 저장하는
  방법도 있지만, 낡은 값을 안 보여주기로 하면 "왜 낡았는지" 설명할 일이 없어 저장이 값을
  하지 않는다. 보여주면 오히려 비교표에서 기준이 다른 합계가 섞인다
- **프로필을 새로 넣을 때 죽은 캐시를 청소한다.** 지문이 갈리면 옛 결과는 사라지는 게 아니라
  다른 키로 남는다. 갱신할수록 저장소에 쌓이므로 현재 프로필과 안 맞는 항목을 그때 지운다
- **결과에는 프로필 수집 시각을 함께 남긴다.** 문자열 하나라 공짜고, 기록 탭에서 "이 딜량은
  언제 기준이었나"를 답할 수 있다

남은 것:

- 덱 이름·메모, 덱 순서 바꾸기, 덱 단위 복제 (편성안 단위 복제는 넣었다)
- 풀 정렬 선택
- 버스트 우선순위가 배열 순서라는 것을 UI에서 드러내기 (지금은 슬롯 순서가 곧 우선순위인데
  화면에 설명이 없다)
- 계산 실패 메시지를 캐릭터 단위로 짚어주기
- PC에서 이미 돌린 결과(`.report-work/*/result.data.json`)를 빌드 시 캐시로 실어 보내기.
  값이 폰·PC 동일하므로(§5) 그대로 쓸 수 있고, 첫 실행부터 캐시가 채워진다

### 3단계 계산 방식

```
예상딜 = 실측딜 × (1 + 계산상 Δ%)
```

계산기는 비율만 내고 기준선은 실측이 잡는다. 덱마다 실측/계산 비율이 달라도 각 덱 자기
실측값에만 곱하므로 문제없다. 덱별 딜량만 있으면 되고 캐릭터별 기록은 필요 없다.

**단서**: 바꾸는 축이 곱연산이고 운용과 얽히지 않을 때만 성립한다. 스킬 레벨·장비 옵션·큐브는
잘 맞지만, 버스트 쿨감처럼 **사이클 수 자체를 바꾸는 축**은 실전 손실이 비례하지 않아
과대평가된다. 보고서에 축별로 표시한다.

작업량의 대부분은 비용표 수집이었다. 어느 축이 채워졌고 무엇을 일부러 뺐는지는
`cost/README.md`가 정본이라 여기에 옮겨 적지 않는다. 서로 대체 불가한 재화를 하나로
합치지 않는 기존 원칙은 유지한다.

### 오버로드 옵션은 다른 축과 다르다

스킬 레벨은 목표치에 정확히 도달하지만 오버로드 옵션은 **기대값이 높은 전략을 고르는**
문제다 — 목표에 닿기 전에 만족하기도 하고 목표보다 좋은 것이 나오기도 한다. 그래서
"한 칸 올리는 데 얼마" 표에 안 들어간다. 계산의 정본은 `overload/`이고 근거·검증 수치는
`overload/README.md`에 있다. 여기에는 **웹앱이 무엇을 보여줄지**만 적는다.

- **UI는 목표를 받지 않는다.** 유저가 "우코+공+장탄을 맞추겠다"를 미리 적는 대신, 지금
  붙어 있는 옵션을 넣으면 계산기가 *그만두기 / 효과변경 / 수치변경* 셋의 가치를 내고
  최선을 고른다. 잠금 조합도 계산기가 정한다. 목표를 받으면 "목표보다 좋은 게 나왔을 때"와
  "목표엔 못 미쳐도 그만두는 게 나을 때"를 표현할 수 없다
- **유저가 정하는 손잡이는 λ(모듈 1개 = 딜 몇 %) 하나다.** 화면에는 λ를 그대로 내보내지
  말고 **"모듈 몇 개까지 쓸 생각인가"**로 받는다. 효율 곡선을 미리 구우면 둘은 1:1로
  역산된다. λ는 한계 교환비이므로 평균 교환비(총딜/총모듈)와 섞어 읽으면 안 된다
- **화면의 단위는 부위 하나가 아니라 후보 목록이다.** 유저가 25인 중 여러 부위를 후보로
  집는다(A의 머리, B의 다리…). 계산기는 그것들을 **우선순위 표**로 돌려준다.
  순위는 λ를 높은 데서 내리며 부위가 들어오는 순서다 — 단순 정렬이 아니라 예산 배분의
  정답이고, "선택된 부위의 기대 모듈 합 = 가진 모듈"이 되는 λ가 곧 답이다
- **캐시 단위는 부위가 아니다.** 한계가치 측정은 *덱 + 상대 + 육성*에 딸리고(5명치가 한
  번에 나온다), 정책 DP는 *캐릭터 + λ*에 딸린다. 부위의 현재 옵션은 조회할 때만 쓰므로
  같은 캐릭터의 머리·몸통·팔·다리는 계산을 공유한다. 그래서 비용이 후보 수가 아니라
  **덱 수와 캐릭터 수**를 따른다
- **폰에서는 거친 레벨 버킷을 쓴다.** 정확도 −1.6%지만 순위를 뒤집을 크기가 아니고,
  어차피 λ 자체가 유저의 어림값이다. 정확한 쪽은 PC 몫이다
- **한계가치 순위표를 먼저 띄운다.** 무거운 것은 한계가치 측정뿐이라, 그게 끝나면
  (`우코 3.60 > 공 2.26 > …`) 유저가 궁금해하던 것의 절반은 이미 답이 나온다.
  정책 DP는 그 뒤에 백그라운드로 돌린다

붙이기 전에 손봐야 하는 것이 하나 있다. 지금 부위의 가치는 *기본 배경 위에 얹는 값*으로
계산되는데, 실제로는 **그 부위를 뺀 나머지 11줄이 배경**이어야 한다(안 그러면 이미 붙어
있는 줄을 두 번 센다). 곱셈 구조라 재측정 없이 `worth(12줄) − worth(그 부위를 뺀 9줄)`로
빼면 되고 DP 구조도 그대로지만, 검증이 필요하다.

같은 캐릭터의 부위끼리는 서로 값을 밀어올리므로 이 표는 **한 시점의 스냅샷**이다. 한
부위를 끝내면 다시 뽑는다. 다른 캐릭터끼리는 안 섞인다 — 솔로레이드 5덱은 캐릭터가
겹치지 않아 한 캐릭터가 한 덱에만 속한다.

## 7. 열린 질문

- 기록 백업 주기와 수단. 브라우저 저장소가 날아가면 기록이 사라진다
- 니케 풀의 기본 필터 — 파싱된 전체 / 보유분 / 육성된 것만
- 공개 상태에서 기록 탭을 어떻게 다룰지 (공개 시점은 4단계로 확정)

## 8. 세션 재개 지침

1. `AGENTS.md` → 이 문서 순으로 읽는다. 게임 메커니즘이 필요할 때만 `context/`로 간다.
   웹앱 빌드·실행 방법은 `web/build.py` 상단 docstring에 있다 (여기에 옮겨 적지 않는다)
2. **웹앱 작업 중 계산기 코드·데이터를 고치지 않는다.** 결과가 이상하면 별도 버그로 보고한다
3. **개인 데이터 경계를 넘지 않는다.** 프로필·기록·세션 쿠키는 어떤 경우에도 커밋 대상이 아니다
4. 단계 상태가 바뀌거나 결정이 뒤집히면 이 문서를 갱신한다. 코드에서 판정 가능한 사실
   (파일 목록, 구현 현황)은 여기에 적지 않는다
