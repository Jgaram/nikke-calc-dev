# 장비 갈래 — raw에는 셋 다 있었다

**2026-08-21 해결.** 원래 이 문서는 "프로필이 T9 기업과 오버로드를 못 가른다"였다.
raw에 가르는 필드가 있었고(`{부위}_equip_corporation_type`), 갈래 구분·비용 갈래를 고치다
더 큰 것이 드러났다 — **T9 기업 장비가 계산에 30% 이상 낮게 들어가고 있었다.** 인게임 식까지
찾아 셋 다 제대로 계산하게 고쳤다. 남은 것은 없다. 이 문서는 경위와 근거로 남긴다.

## 갈래는 넷이다

| 인게임 | raw API | 프로필 | 계산기 |
|---|---|---|---|
| 미장착 | `equip_tier` 0 | `{"tier": "없음"}` | 0 |
| 일반 T1~T9 (강화 없음) | tier 1~9 · `corporation_type` 0 | `{"tier": "T9"}` 등 | `일반` 표 |
| **T9 기업** (강화 0~5) | tier 9 · `corporation_type` **1~4** · `equip_lv` 0~5 | `{"tier": "T9", "corp": 제조사, "level": n, "_track": "T9"}` | `일반` 표 × 아래 식 |
| **오버로드** (강화 0~5) | tier **10** · `corporation_type` 0 · 옵션 1~3줄 | `{"level": n, "_track": "오버로드"}` | `기업` 표 (관측값) |

`equip_tid`는 갈래를 담지 않는다 — `3`+부위+클래스+`0`+티어+`01` 꼴이라 부위·클래스·티어뿐이다.
T9 기업 장비와 일반 T9는 **tid까지 같고**(둘 다 `3110901` 등) `corporation_type`만 다르다.
그래서 갈래는 `equip_tier` + `corporation_type`이 가른다.

### 실측 교차표

`profiles/me.raw.json`(수집 2026-08-21, 193종 × 4부위 = 772부위)을
`(equip_tier, corporation_type, equip_lv, 옵션 줄 수) → 부위 수`로 세면:

```
(0, 0, 0, 0) 538      미장착
(9, 0, 0, 0)   2      일반 T9 — 제조사 없음, 강화 없음
(9, 1~4, 0, 0) 37     T9 기업, 강화 0
(9, 1, 1, 0)   1      T9 기업, 강화 1   ← 일반 장비에는 없는 값
(9, 1, 5, 0)   3      T9 기업, 강화 5
(9, 3, 5, 0)   1      T9 기업, 강화 5
(10, 0, 0~5, 1~3) 190 오버로드 — 전부 corporation_type 0이고 전부 옵션이 붙어 있다
```

**인게임 확인 완료 (2026-08-21, 유저)**: 위 판독이 전부 맞다. 그리고 오버로드는
`기업 T9 5강 → 모듈 1개로 개조`라서 **옵션 첫 줄이 100% 붙는다** — 옵션 유무로 갈라도
맞는다. 그래도 `corporation_type` 쪽을 쓴다: 옵션 유무는 결과의 부산물이고 이쪽은 원인이며,
제조사까지 함께 준다.

## 인게임 계산식 (사이트 번들에서 확인)

blablalink 프론트(`index-*.js`의 `getEquipAttr`)가 쓰는 식은 이렇다.

```
스탯 = round( 기본값 × (1 + 0.3×(장비 제조사 == 캐릭터 기업) + 0.1×강화단계) )
```

- 기본값 = CDN `ItemEquipTable-ko.json`의 그 tid·클래스·부위 `stat` (우리 표의 `일반`/`기업`)
- `0.3` = `settings_equip_corp_bounus`, `0.1` = `settings_equip_increase_bouns`
  (둘 다 `setting-*.js` 상수). **곱이 아니라 합**이다 — 기업 5강은 ×1.8이지 ×1.3×1.5가 아니다.
  인게임 확인: 슈가 머리(테트라 5강) 공격력 **5,819** = round(3233×1.8) ✅
  (곱이었다면 6,304). 검증값은 `context/DATA_VERIFY.md §장비`
- `corporation_type` = 프론트의 `equip_manufacturer_bonus`. 이름 그대로 **제조사 보너스 자격**이고
  캐릭터 기업과 같을 때만 붙는다. 코드는 `{1: 엘리시온, 2: 미실리스, 3: 테트라, 4: 필그림,
  7: 어브노말}` (실측으로 1~4 확인, 7은 번들에만 있고 미관측)
- 실측 42부위 **전부** 장비 제조사 = 캐릭터 기업이었다. 안 맞는 기업 장비는 끼지 않는 게 보통이다
- 오버로드는 `corporation_type`이 0이라 **제조사 보너스를 안 받는다**. T10 기본값이 이미
  T9의 1.86배라(화력형 머리 atk 3233 → 6014) 기업 5강(3233×1.8=5819)보다 높다 — 앞뒤가 맞는다

**우리 표는 이 식과 이미 맞는다.** `equipment_stats.json`의 `기업`(=오버로드) 1~5단계는
"인게임 관측 선형 보간"으로 적어 뒀는데 `기본값×(1+0.1n)`과 대조하면 72칸 중 43칸이 정확히
같고, 어긋난 24칸은 ±1(반올림)이다. 보간이 아니라 **원래 그 식이었다.**

예외는 **지원형 다리 hp** 하나뿐이다 — 관측값이 식보다 1~5단계에서 7·12·18·24·31씩 낮다
(−0.05%). `cdn_tables.py`가 "단계당 정확히 +10%가 아닌 부위가 있다"고 적어 둔 그 부위이고,
관측이 더 정확하므로 **표는 그대로 둔다.**

## 고친 것 (2026-08-21)

1. **계산기** — `calculator/base_stat._equip_stat()`이 갈래 셋을 가른다. `tier`+`corp`면
   T9 기업이라 일반 표의 기본값에 `(1 + 0.3×기업일치 + 0.1×강화)`를 곱하고(부위 단위 반올림),
   `tier` 없으면 종전대로 `기업`(오버로드) 관측표를 `level`로 조회한다. 캐릭터 기업이 필요해
   인자가 하나 늘었다(`meta["manufacturer"]`를 넘긴다).
   `기업` 표는 **건드리지 않았다** — 관측값이 식보다 정확하고(지원형 다리 hp) 식으로
   갈아엎으면 golden baseline이 이유 없이 움직인다.
2. **프로필** — `scraper/profile_fetch.py::_equipment()`가 T9 기업 부위에 `corp`·`level`을
   적는다(계산기가 읽는 키). 비용 갈래는 `_track`으로 따로 적는다 — `_`로 시작해
   `spec.deep_merge()`가 걸러내므로 계산에는 안 들어가고, 값은 `cost/tables.json`의
   `장비강화.레벨도달비용` 키(`T9`·`오버로드`)와 글자까지 같아 웹앱이 그대로 조회한다.
3. **웹앱** — `web/src/growth.js`가 부위마다 그 갈래로 한 칸 비용을 고른다
   (`gearExp(track, level)`). T9 기업 부위도 이제 **강화 축이 선다** — 계산기가 반응하므로
   Δ를 잰다. 표식이 없는 낡은 프로필만 비싼 쪽(오버로드)으로 잡고 그 사실을 줄 아래 적는다.
   `내 육성`은 `T9 테트라 3강`처럼 제조사까지 적고, 캐릭터 기업과 안 맞는 장비는 따로 알린다.
4. **프로필을 다시 받아야 반영된다** (`profile-sync`). `_track`은 `_` 키라 육성 지문
   (`app.js`의 `stable()`)에 안 들어가지만 `corp`·`level`은 들어가므로, 해당 16명이 낀 덱의
   기존 계산 결과는 지문이 갈려 다시 계산된다(그게 맞다 — 스탯이 실제로 올랐다).

검증: golden snapshot 27/27 통과(기본 스펙은 오버로드라 경로가 안 바뀐다), `doclint` OK.
비용 숫자도 그대로다 — 실측 계정의 오버로드 190부위는 갈래가 안 바뀌었다.

**스탯이 오르는 캐릭터** — 실측 42부위(16명). 지금까지 이만큼 낮게 계산되고 있었다.

| 캐릭터 | 부위 | 장비 | 배수 | 공격력 |
|---|---|---|---|---|
| 솔린 : 프로스트 티켓 | 머리·몸통·팔 | 엘리시온 5강 | ×1.0 → ×1.8 | 95,733 → 99,652 (+4.1%) |
| 슈가 | 머리 | 테트라 5강 | ×1.0 → ×1.8 | 121,354 → 124,733 (+2.8%) |
| 헬름 : 아쿠아마린 | 다리 | 엘리시온 1강 | ×1.0 → ×1.4 | 105,959 → 107,722 (+1.7%) |
| (나머지 37부위, 13명) | — | 강화 0 | ×1.0 → ×1.3 | — |

42부위 전부 장비 제조사 = 캐릭터 기업이라 전부 +30%를 받는다(안 맞는 장비를 끼면
`profile_fetch`가 알린다). 공격력은 프로필 적용·레벨 400 고정 기준이다.

## 남은 잔가지

- **어브노말 기업 장비(코드 7)는 미관측이다.** 사이트 번들에만 있고 실측 42부위는 1~4뿐이었다.
  모르는 코드가 오면 `profile_fetch.py`가 `?7`로 적고 경고한다 — 계산기는 그 이름이 캐릭터
  기업과 안 맞으니 보너스를 안 준다(안전한 쪽)
- **오버로드 장비의 제조사는 raw가 0으로 준다.** 실측 190부위 전부 그렇다. 그래서 오버로드는
  제조사 보너스를 안 받는 것으로 계산한다 — T10 기본값이 T9의 1.86배라 이미 그만큼 얹혀 있다
- 이 변경은 **웹앱 작업 중 계산기 동결**(`webapp-roadmap.md` §8)의 예외다. 인게임 식이
  확인돼 표를 늘리지 않고 고칠 수 있었고, golden baseline이 안 움직이는 것도 확인했다
