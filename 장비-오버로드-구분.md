# 장비 등급을 기업/오버로드로 구분하지 못한다

**미해결.** 2026-08-21에 웹앱 육성 탭(3단계)을 붙이다 드러났다.

인게임 장비 갈래는 셋인데 `profiles/*.json`은 둘까지만 구분한다.

| 인게임 | raw API | 프로필 | 계산기 |
|---|---|---|---|
| 미장착 | `equip_tier` 0 | `{"tier": "없음"}` | 0 |
| 일반 T1~T9 (강화 없음) | `equip_tier` 1~9 | `{"tier": "T9"}` 등 | `일반` 표 |
| T9 기업 (강화 0~5) | `equip_tier` 10 | `{"level": n}` | `기업` 표 |
| **오버로드 (강화 0~5)** | `equip_tier` **10** | `{"level": n}` ← **같다** | `기업` 표 ← **같다** |

기업 장비를 오버로드 변환하면 옵션 줄이 붙고 강화 갈래가 바뀌는데, `equip_tier`는 그대로
10이다. 프로필도 계산기도 그 둘을 한 칸에 넣는다.

## 원본에는 구분이 있다

`equip_tid`가 아니다 — tid는 `3`+부위+클래스+`0`+티어+`01` 꼴이라 부위·클래스·티어만 담는다
(실측: 머리 T10 `3111001`/`3121001`/`3131001`, 다리 T9 `3410901`…). 등급 자리가 없다.

구분이 남는 곳은 **옵션 슬롯**뿐이다.

```
{부위}_equip_option{1,2,3}_id 가 하나라도 0이 아니면 → 오버로드 장비
전부 0이면 → 변환 전 T9 기업 장비
```

오버로드 변환이 옵션 줄을 붙이는 유일한 경로라서다. 실측(`profiles/me.raw.json`,
수집 2026-08-19): tier 10인 부위 **190개 전부**가 옵션을 갖고 있었다 — 이 계정에는
변환 전 기업 장비가 한 부위도 없다.

`scraper/profile_fetch.py`의 `_equip_skills()`가 이 슬롯들을 이미 읽지만 **4부위 합산**으로
접어 `equip_skills`에 넣는다. 그래서 부위별 판정이 프로필에서 사라진다.

## 지금 무엇이 틀리는가

### 1. 장비 강화 비용 (웹앱 육성 탭)

`cost/tables.json`의 `장비강화.레벨도달비용`은 갈래가 둘이고 값이 1.5배 차이 난다.

| 한 칸 | T9 | 오버로드 |
|---|---:|---:|
| 3→4 | 400,000 | 600,000 |
| 0→5 누적 | 1,592,000 | 2,388,000 |

웹앱은 구분할 수 없으니 **비싼 쪽(오버로드)으로 잡고** 그 사실을 줄 아래에 적는다
(`web/src/growth.js`의 `GEAR_TRACK`). 실계정이 전부 오버로드라 지금은 맞지만, 변환 전
장비를 낀 캐릭터가 섞이면 그 줄만 비용이 1.5배로 과대평가된다 — 효율 순위가 그만큼
낮게 나온다.

`cost.gear(track, ...)`는 갈래를 인자로 받으므로 **비용표 쪽은 고칠 게 없다.** 부르는 쪽이
갈래를 모를 뿐이다.

### 2. 플랫 스탯 (확인 안 됨)

`data/base_stat_tables/equipment_stats.json`의 `기업`은 T10 한 세트뿐이고, 오버로드 장비도
같은 표로 계산된다. CDN(`/equip/ItemEquipTable-ko.json`)이 `item_rare: "T10"`을 한 세트만
주기 때문이다(`scraper/cdn_tables.py`의 `CORP_RARE`).

그런데 CDN이 주는 것은 **강화 0단계뿐**이고, 1~5단계는 인게임 관측(0·5단계)에서 선형
보간한 값이다. 그 관측이 **어느 쪽 장비였는지가 어디에도 안 적혀 있다** — 근거는
`cdn_tables.py::build_equipment_table`의 주석 한 줄이 전부고 `context/DATA_VERIFY.md`에
항목이 없다. 계정 상태로 미루면 오버로드였을 가능성이 크고, 그렇다면 표는 오버로드
기준이며 변환 전 기업 장비가 틀리게 계산된다.

**둘의 플랫 스탯이 실제로 같은지부터 인게임에서 확인해야 한다.** 같으면 표는 그대로 두고
1번만 고치면 되고, 다르면 표를 갈래별로 쪼개야 한다.

## 고치는 순서

1. **인게임 확인** — 변환 전 기업 장비와 오버로드 장비의 같은 부위·같은 강화 단계
   플랫 스탯을 비교한다. 결과를 `context/DATA_VERIFY.md`에 남긴다
2. **프로필에 표식을 남긴다** — `profile_fetch.py::_equipment()`가 옵션 id를 보고
   `{"level": n, "_overload": true}`를 적는다. `_` 접두사면 `spec.deep_merge()`가 시뮬에
   넘기지 않고 `spec.GROWTH_KEYS` 검사도 통과하므로 계산에는 아무 영향이 없다.
   프로필 재수집이 필요하다 (`profile-sync` skill)
3. **웹앱이 그 표식을 읽는다** — `growth.js`의 `GEAR_TRACK` 고정값을 부위별 판정으로
   바꾸고, 줄 아래 단서 문구를 뗀다
4. 1번 결과가 "다르다"면 `equipment_stats.json`을 갈래별로 쪼갠다. 이건 계산기 쪽 변경이라
   `context/snapshot.py` golden baseline이 함께 움직인다

1~3은 서로 독립이다. 2·3만 해도 비용 쪽 과대평가는 사라진다.

## 하지 않기로 한 것

웹앱 작업 중에는 계산기 코드·데이터를 고치지 않는다(`webapp-roadmap.md` §8). 그래서
2026-08-21에는 **보수적인 기본값 + 화면 표시**까지만 하고 이 문서를 남겼다.
